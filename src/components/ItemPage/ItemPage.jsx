import React from 'react';

const ItemPage = ({ game }) => {
  return (
    <li>
      {/* img, title, name, rate, courtname, time */}
      <p>{game.title}</p>
    </li>
  );
};

export default ItemPage;
<itempage></itempage>;
